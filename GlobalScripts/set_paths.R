i_am("GlobalScripts/set_paths.R")

global            <- config::get(config = "default")
libprep           <- config::get(config = "libprep")
rapid16s          <- config::get(config = "rapid16s")
lsk               <- config::get(config = "lsk")